---

## 🔹 README pour HenriF (Courses / inscription aux cours)

```markdown
# Branch: HenriF-feature
# Module: Courses / Inscriptions aux cours

Salut HenriF 👋,

## 📌 Règles principales

1. Travailler uniquement sur `HenriF-feature`.
2. Ne jamais toucher les branches des autres ou `main`.
3. Se référer au README de `main` pour les instructions globales.

## 🔹 Workflow quotidien

1. Mettre à jour sa branche avec `main` avant de commencer :

```bash
git checkout HenriF-feature
git fetch origin
git merge origin/main

Développer les fonctionnalités liées aux cours et inscriptions.

Commit régulier et clair :

git add .
git commit -m "Ajout / modification de <fonctionnalité>"

Pousser sur GitHub :

git push origin HenriF-feature

Créer une PR vers main après validation et tests.

⚠️ Rappel

Ne pas toucher aux autres branches.

Tout doute → contacter l’admin.
