
---

## 🔹 README pour MekemeB (UI / interface console)

```markdown
# Branch: MekemeB-feature
# Module: UI / Interface console / Affichages

Salut MekemeB 👋,

## 📌 Règles principales

1. Travailler uniquement sur `MekemeB-feature`.
2. Ne jamais toucher aux autres branches ou `main`.
3. Consulter le README de `main` pour les bonnes pratiques.

## 🔹 Workflow quotidien

1. Mettre à jour sa branche avec `main` :

```bash
git checkout MekemeB-feature
git fetch origin
git merge origin/main

Développer l’interface console et les affichages.

Commit clair et régulier :

git add .
git commit -m "Ajout / modification de <fonctionnalité>"

Pousser sur GitHub :

git push origin MekemeB-feature

PR vers main uniquement après tests et validation.

⚠️ Rappel

Ne pas toucher aux autres branches.

Doute → contacter l’admin.
